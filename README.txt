PRODUCT REQUEST FORM - MOBILE PWA C33

How to test:
1. Extract the ZIP.
2. Open index.html with Chrome or Edge, or upload all files to GitHub Pages.
3. On Android Chrome: tap the three dots > Add to Home screen.
4. On iPhone Safari: tap Share > Add to Home Screen.

C33 changes:
- 3D viewer files and 3D PDF code have been fully removed.
- Somfy IO remote control options use 40 Channels instead of 16 Channels.
- No radio / checkbox choice is preselected by default.
- Select fields start blank unless there is no available group/subgroup; then they show None/Yok.
- Packaging Type and Loading use single-select checkbox-style options.
- Pergola forms include water outlet detail, outlet direction and pipe length fields.
- Color catalog labels and fabric descriptions follow the selected preview language.
- PDF download/share output remains English.

No server, no login, no database. User data is saved locally on the device.


C33-v2 cache fix:
- Service worker cache version bumped.
- App/order draft storage version bumped so old random selections do not come back.
- Script and stylesheet references include cache-busting query strings.
- 3D viewer references remain removed.

C38 changes:
- Color/fabric picker panel height stays stable while searching; only the list content filters.

- C40: Mobile auto-advance scroll after completing fields and single selections.

C43:
- B-Cube Galaxy and Space motor options updated to T-Motion Somfy RTS options.
- Color/fabric catalog search bar stays fixed while scrolling.

C44: Dynamic placeholders follow selected language; mobile section headings are stronger; cache bumped.


C57 Janela Cassette Awning: detailed form for Janela based on price list dimensions, colors, control, valance/printing, roof, sensors and packing.
